import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>Generateur de données <span className="red">utilisateur.</span></h1>
        </header>
    );
}

export default Header;